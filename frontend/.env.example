# Point the frontend at your backend. If omitted, defaults to http://localhost:8000.
# If the backend is down, the app falls back to the static files in public/data.
VITE_API_BASE=http://localhost:8000
